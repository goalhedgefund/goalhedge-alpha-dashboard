{
  "name": "scalper",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "core",
    "ui"
  ],
  "scripts": {
    "test": "npm run test -w core && npm run test -w ui",
    "typecheck": "npm run typecheck -w core && npm run build -w ui",
    "lint": "npm run lint -w core && npm run lint -w ui",
    "demo": "npm run build -w @scalper/core && npm run demo -w @scalper/core",
    "paper:live-data:dhan": "npm run build -w @scalper/core && npm run paper:live-data:dhan -w @scalper/core",
    "ui": "npm run dev -w @scalper/ui",
    "e2e": "npm run e2e -w @scalper/ui"
  }
}
